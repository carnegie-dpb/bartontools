## given an input data frame that has AT loci as row names, count the number that do and don't have significant limma time results for the given schema, condition

count.limma = function(schema, condition, df) {



  
}
