<%
// the schema for this experiment
String SCHEMA = "gse30703";

// bail if more than this many matches is reached
int MAXMATCHES = 1700;

// the experiment object holds all the metadata
Experiment experiment = new Experiment(application, SCHEMA);  
%>
